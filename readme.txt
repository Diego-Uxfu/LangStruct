backend stores the logic for the cfg,crf,fsms
frontend folder stores the logic for the website
to run successfully, cd into backend and run api.py(this rovides endpoints for the frontendnt9 call to load words)
then cd into frontend and npm install then npm run dev to run the frontend
both api.py and frontend must be running simultaneously for the project tonwork